---
interface Props {
  as?: "body" | "main" | "article";
  class?: string;
}

const {
  as: Element = "div",
  class: className,
  "data-prose": dataProse = "plaintext",
  ...props
} = Astro.props;
---

<Element {...props} data-prose={dataProse} class={className}>
  <slot />
</Element>
